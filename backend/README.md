# Backend — reference snapshot

The folder **`reference/`** in this directory holds a **read-only snapshot** of the `architecture/` tree from the **DX-LAB-architecture** repo. It is **not** imported at runtime.

Use it for documentation, comparison, and onboarding only. For product behavior and the live ACP hub, edit **`acp_hub/`** instead of editing files under `reference/`.
